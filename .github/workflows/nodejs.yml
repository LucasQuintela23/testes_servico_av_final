name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20.x'

    - name: Install dependencies
      run: npm install

    - name: Rodar testes unitários (mocados)
      run: npm run test:unit

    - name: Subir a API
      run: npm start &

    - name: Esperar a API estar funcionando
      run: sleep 5

    - name: Rodar testes externos de API via HTTP
      run: npm run test:external
